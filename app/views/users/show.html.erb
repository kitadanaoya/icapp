<% provide(:title, @user.name) %>

<div class="row">
  <side class="col-xs-4 text-center col-md-3">
    <div>
      <%= gravatar_for @user %>
    </div>
    <ul>
      <li><%= @user.name %></li>
      <li><%= @user.email %></li>
    </ul>
  </side>
  
  <div class="col-xs-8 col-md-9">
    <%= @user.point_ids %>
    <%= sum_for @user %>
  </div>

</div>